# 📘 FlexWeb v2.0 完全 API 仕様書

FlexWeb は「最小・最速・最軽量」を追求した DOM 操作ライブラリです。
本ドキュメントでは、v2.0 の全 API を網羅的に解説します。

---

## 1. 核心概念

### Q (DOM カーソル)
`Q` 関数は DOM を指し示す「カーソル」を返します。すべての操作はこのカーソルを通じて行われます。

### el (DOM 生成・コンポーネント)
`el` は DOM を生む唯一のインターフェースです。文字列を渡せば DOM を生成し、関数を渡せばコンポーネントとして実行します。

---

## 2. グローバル API

### `Q(selector)`
DOM 要素を選択し、`QCollection` (カーソル) を返します。

- **引数**:
  - `selector`: `string` (CSSセレクタ), `Node`, `Node[]`, または `QCollection`
- **動作**:
  - `string`: `querySelectorAll` を実行。
  - `Node`: 単一要素のカーソルを生成。
  - `QCollection`: そのまま返す。

### `Q.state(initialValue)`
リアクティブな状態オブジェクトを生成します。

- **引数**:
  - `initialValue`: 初期値
- **返り値**:
  - `Proxy` オブジェクト。`.value` プロパティで値の読み書きが可能です。

---

## 3. QCollection (カーソル) メソッド

### `.el(arg, ...args)`
要素の生成またはコンポーネントの呼び出し。

#### パターン A: DOM 生成
- **引数**: `string` (例: `"div#id.class"`)
- **動作**: 
  1. 文字列をパースして要素を作成。
  2. 現在のカーソルが指す要素に `appendChild` する。
- **返り値**: 新しく生成された要素を指す `QCollection`。
- **タグ短縮形**:
  - `sec` → `section`, `hdr` → `header`, `btn` → `button`, `art` → `article`, `ftr` → `footer`, `inp` → `input`, `txt` → `textarea`, `lbl` → `label`, `spn` → `span`

#### パターン B: コンポーネント呼び出し
- **引数**: `function(parent, ...args)`
- **動作**: 関数を実行。第一引数 `parent` には現在のカーソルが自動的に渡されます。
- **返り値**: 関数の返り値。

### .text(value) / .content(value)
テキスト内容の操作。

- **引数**: `string` (省略時は取得)
- **返り値**: 設定時は `this`、取得時は `string`。

---

### .add(selector) (v2.1.0)
クラスや ID を追加します。

- **引数**: `string` (例: `".active.blue"`, `"#new-id"`)
- **動作**: 
  - セレクタに含まれるクラスを `classList.add` します。
  - セレクタに ID が含まれる場合、`id` プロパティを上書きします。

---

### .rm(selector) (v2.1.0)
クラスや ID を削除します。

- **引数**: `string` (例: `".active"`, `"#old-id"`)
- **動作**:
  - セレクタに含まれるクラスを `classList.remove` します。
  - セレクタに ID が含まれ、かつ現在の要素の ID と一致する場合、ID を空にします。

---

### .set(selector) (v2.1.0)
クラスや ID を完全に置き換えます。

- **引数**: `string` (例: `"#new-id.only-this-class"`)
- **動作**:
  - `className` をセレクタのクラス群で完全に上書きします。
  - ID が含まれる場合、`id` プロパティを上書きします。

---

### .fx(selector) (v2.2.0)
CSS トランジション / アニメーションを開始し、終了を待機します。

- **引数**: `string` (クラスや ID)
- **動作**:
  - 指定したセレクタを `add` します。
  - `transitionend` または `animationend` イベントが発生するまで待機する **Promise** を返します。
  - アニメーションが定義されていない場合は即座に resolve されます。
- **返り値**: `Promise<QCollection>`

---

### .html(value)

innerHTML の操作。

- **引数**: `string` (省略時は取得)

---

### `.attr(name, value)`
属性の操作。

- **引数**: 
  - `name`: 属性名
  - `value`: 値。`null`, `false`, `undefined` を渡すと属性を削除します。

---

### `.on(type, handler)`
イベントリスナーの登録。

- **引数**: 
  - `type`: イベント名 (`"click"`, `"input"` など)
  - `handler`: `(el, event) => void`
- **特殊イベント**:
  - `"hover"`: `handler(el, isHovering, event)` が呼ばれます。

---

### `.ui(styleObj)`
インラインスタイルの適用。

- **引数**: `{ [prop]: value }`
- **特別キー**:
  - `bg`: `background`
  - `text`: `color`

---

### `.bind(state, mapFn, mode)`
状態（State）と UI を同期させます。

- **引数**:
  - `state`: `Q.state` で作ったオブジェクト
  - `mapFn`: `(value) => result` 変換関数
  - `mode`: `"text"`, `"html"`, `"attr"`, `"class"`, `"css"`
- **動作**: `state.value` が変わるたびに `mapFn` が実行され、UI が更新されます。

---

## 4. コンポーネントの設計パターン

FlexWeb v2.0 では、コンポーネントは「親を受け取り、子を追加する関数」として定義されます。

```javascript
// 定義
function Card(parent, data) {
  const root = parent.el("art.card");
  root.el("h3").text(data.title);
  root.el("p").text(data.body);
  return root; // 生成したルートを返すと便利
}

// 利用
Q("#container").el(Card, { title: "Hello", body: "World" });
```

---

## 5. v1.x からの移行ガイド

1.  **`Q.add()` は非推奨**: 代わりに `Q(parent).el("tag")` を使用してください。
2.  **`Q.mount()` は非推奨**: `Q(target).el(Component)` を使用してください。
3.  **`content()` は `text()` へ**: より直感的な `text()` を推奨します。
4.  **Emmet 短縮形**: `Q.add("section")` は `el("sec")` と書けるようになりました。
